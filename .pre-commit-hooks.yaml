- id: systemrdl-to-ipxact
  name: Convert SystemRDL to IP-XACT
  description: Compile regmap/**/*.rdl via the peakrdl-ipxact Python API and stage the XML output.
  entry: systemrdl-to-ipxact
  language: python
  files: ^regmap/.*\.rdl$
  pass_filenames: true

- id: asciidoc-addrmap
  name: Render first-level address-map AsciiDoc table
  description: Generate an AsciiDoc table of first-level addrmap/mem children for each regmap/**/*.rdl.
  entry: asciidoc-addrmap
  language: python
  files: ^regmap/.*\.rdl$
  pass_filenames: true

- id: ipxact-to-systemrdl
  name: Convert IP-XACT XML to SystemRDL
  description: Import an explicit IP-XACT XML file and emit it as a SystemRDL (.rdl) source.
  entry: ipxact-to-systemrdl
  language: python
  pass_filenames: false
  always_run: true

- id: systemrdl-to-verilog
  name: Generate SystemVerilog register block from SystemRDL
  description: Compile regmap/**/*.rdl via the peakrdl-regblock Python API and stage the generated SystemVerilog.
  entry: systemrdl-to-verilog
  language: python
  files: ^regmap/.*\.rdl$
  pass_filenames: true

- id: systemrdl-to-uvm
  name: Generate UVM register model from SystemRDL
  description: Compile regmap/**/*.rdl via the peakrdl-uvm Python API and stage the generated UVM RAL package.
  entry: systemrdl-to-uvm
  language: python
  files: ^regmap/.*\.rdl$
  pass_filenames: true
